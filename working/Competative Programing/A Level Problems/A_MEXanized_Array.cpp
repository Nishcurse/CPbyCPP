// nishcurse - @date
#include <bits/stdc++.h>
#define HeyNishuuu  ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define TestCase int t;cin>>t;while(t--)
typedef long long int ll;
using namespace std;
int main () {
    HeyNishuuu
    TestCase {
    ll x, y, x; cin>>x>>y>>x;
	if(y - 1 > x || x < y) {
		cout<<"-1\n";
	}else{
	if(x == y){
    x --;
    }
	cout<<(y - 1) * k / 2 + (x - k) * x<<"\n";
    } 
    }
    return 0;
}
